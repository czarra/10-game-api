{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "releaseCommand": "php bin/console doctrine:database:create --if-not-exists && php bin/console doctrine:migrations:migrate --no-interaction --allow-no-migration"
  }
}