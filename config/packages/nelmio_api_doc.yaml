nelmio_api_doc:
    documentation:
        info:
            title: 'Geolocation game API'
            description: 'A backend application for creating and managing geolocation-based urban games.'
            version: '1.0.0'
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api(?!/doc$) # Dokumentuj wszystko pod /api, z wyjątkiem samej dokumentacji